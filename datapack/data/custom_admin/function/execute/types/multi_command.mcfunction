# Gerçek liste loop — params.commands:[] array-shift
# temp.ep = player (execute/action tarafından set edildi)
execute if data storage mc:_ temp.params.commands[0] run function custom_admin:execute/types/mc_loop
